apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: cashmate
spec:
  selector:
    app: notification-service
  ports:
    - port: 3006
      targetPort: 3006
  type: ClusterIP