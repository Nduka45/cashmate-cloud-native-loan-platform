apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: cashmate
spec:
  selector:
    app: customer-service
  ports:
    - port: 3001
      targetPort: 3001
  type: ClusterIP