apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: cashmate
spec:
  selector:
    app: payment-service
  ports:
    - port: 3005
      targetPort: 3005
  type: ClusterIP